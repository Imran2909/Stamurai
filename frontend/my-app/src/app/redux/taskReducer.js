
  const initialState = {
    loading: false,
    error: null,
    tasks: []
  };
  
  const taskReducer = (state = initialState, action) => {
    switch (action.type) {
     
      default:
        return state;
    }
  };
  
  export default taskReducer;